

function capitalize(array) {
    let newlist = []
    array.forEach((element) => { newlist.push(element.toUpperCase()) })
    return(newlist)
    }
    
    
    /* displayResult(capitalize(["CHANGE", "this", "array", "BY", "ThE", "RESULT", "oF", "youR", "FUNCTION", "TO", "test", "IT", "OUT"])) */